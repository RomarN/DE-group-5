apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-model-repo
  namespace: ForestFires
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs